twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null
    globals:
        empresaGlobal: '%env(resolve:EMPRESA)%'
    #date:
        #timezone: America/La_Paz

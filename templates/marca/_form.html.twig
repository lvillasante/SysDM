<div class="x_content">
	<div class="col-md-123 center-margin">
		{{ form_start(form, {'attr':{'class': 'formValidate form-horizontal form-label-left'} }) }}
		<div class="form-group row">
			<label>Nombre</label>
			{{ form_widget(form.nombre) }}
		</div>
		<div class="form-group row">
			<label>Signo de la marca</label>
			{{ form_widget(form.signo) }}
		</div>
		<div class="form-group row">
			<label>Color asignado</label>
			{{ form_widget(form.color) }}
		</div>
		<div class="form-group row">
			<label>Asignación</label>
			{{ form_widget(form.asignacion) }}
		</div>

		<div class="ln_solid"></div>
		<div class="item form-group">
			<div class="">
				<button type="submit" class="btn btn-info btn-lg">
					<i class="fa fa-save"></i>
					Guardar</button>
				<a href="{{path('marca_index')}}" class="btn btn-warning">
					<i class="fa fa-ban"></i>
					Cancelar</a>

			</div>
		</div>
		{{ form_widget(form._token) }}
		{{ form_end(form,{'render_rest':false}) }}
	</div>
</div>
